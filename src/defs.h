#ifndef __defs_h__
#define __defs_h__

#define items(x) (sizeof(x) / sizeof(*(x)))

#define true (1)
#define false (0)

#define nil NULL

typedef bool boolean;
typedef int integer;

#endif /* __defs_h__ */